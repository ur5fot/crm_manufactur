# CRM на CSV (Vue + Node)

**[English version / Англійська версія](README.md)**

Локальна CRM-система, яка зберігає дані в CSV-файлах та PDF-документах. CSV-файли можна безпечно редагувати в Excel, зміни автоматично завантажуються при оновленні сторінки.

## Можливості

- **Зберігання в CSV** - не потрібна база даних, редагуйте дані прямо в Excel
- **Динамічний UI** - вся структура форм та таблиць керується через `fields_schema.csv`
- **Послідовні числові ID** - прості ідентифікатори співробітників (1, 2, 3...)
- **Управління файлами** - завантаження та зберігання PDF-документів для кожного співробітника
- **Dashboard** - картки статистики з розкривним списком працівників, таймлайн відпусток з клікабельними посиланнями
- **Відстеження відпусток** - автоматична зміна статусів, сповіщення про початок та закінчення відпусток
- **Зведена таблиця** - inline редагування подвійним кліком, множинні фільтри з підтримкою порожніх значень
- **Автоматичне логування** - всі зміни записуються в `logs.csv` з деталізацією по полях
- **Імпорт CSV** - масове завантаження співробітників з CSV-файлів
- **UTF-8 з BOM** - коректна підтримка кирилиці в Excel

## Технологічний стек

- **Frontend:** Vue 3 + Vite
- **Backend:** Node.js + Express
- **Сховище:** CSV файли (UTF-8 з BOM)
- **Завантаження файлів:** Multer

## Структура проекту

```
crm_manufactur/
├── data/
│   ├── employees.csv              # Основні дані співробітників (40 полів) - в gitignore
│   ├── fields_schema.csv          # Мета-схема: типи полів, мітки, опції, UI конфігурація - в gitignore
│   ├── fields_schema.template.csv # Шаблон схеми для нових інсталяцій (tracked in git)
│   ├── logs.csv                   # Журнал аудиту всіх змін - в gitignore
│   ├── employees_import_sample.csv # Шаблон для імпорту з UTF-8 BOM
├── files/                         # Завантажені PDF-документи - в gitignore
│   └── employee_[ID]/
├── server/                        # Backend на Express.js
└── client/                        # Frontend на Vue.js
```

## Швидкий старт

### Перше встановлення

При першому розгортанні створіть локальний файл схеми з шаблону:

```bash
cp data/fields_schema.template.csv data/fields_schema.csv
```

**Важливо:** `fields_schema.csv` знаходиться в `.gitignore`, щоб ваші локальні зміни схеми (кастомні поля, опції) не конфліктували з оновленнями з git. Редагуйте його під свої потреби!

### Використання скрипта запуску

Запуск серверів:
```bash
./run.sh
```

Зупинка серверів:
```bash
./stop.sh
```

### Ручний запуск

**Термінал 1 (сервер):**

```bash
cd server
npm install
npm run dev
```

**Термінал 2 (клієнт):**

```bash
cd client
npm install
npm run dev
```

Відкрийте `http://localhost:5173` в браузері.

## Формат CSV

- **Роздільник:** `;` (крапка з комою) для сумісності з Excel
- **Кодування:** UTF-8 з BOM для коректного відображення кирилиці в Excel
- **Рядок заголовків:** Повинен залишатися незмінним
- **ID співробітників:** Послідовні числа (1, 2, 3...) замість UUID
- **Шляхи до файлів:** Зберігаються як відносні шляхи типу `files/employee_1/passport.pdf`

## Модель даних

### Основні таблиці

- **employees.csv** - Основні записи співробітників (40 полів):
  1. `employee_id` - ID співробітника (автоінкремент)
  2. `last_name` - Прізвище
  3. `first_name` - Ім'я
  4. `middle_name` - По батькові
  5. `employment_status` - Статус роботи (Працює, Відпустка, Звільнений, Лікарняний)
  6. `additional_status` - Додатковий статус
  7. `location` - Місцезнаходження
  8. `department` - Підрозділ
  9. `position` - Посада
  10. `grade` - Розряд (словами)
  11. `salary_grid` - Зарплатна сітка
  12. `salary_amount` - Оклад
  13. `specialty` - Спеціальність
  14. `work_state` - Робочий стан
  15. `work_type` - Тип роботи (Повна ставка, Часткова зайнятість, Контракт)
  16. `gender` - Стать (Чоловіча, Жіноча)
  17. `fit_status` - Придатність (Придатний, Непридатний, Обмежено придатний)
  18. `order_ref` - Наказ
  19. `bank_name` - Банк
  20. `bank_card_number` - Номер картки
  21. `bank_iban` - IBAN
  22. `tax_id` - ІПН
  23. `email` - Ел. пошта
  24. `blood_group` - Група крові (I (0), II (A), III (B), IV (AB))
  25. `workplace_location` - Місце роботи
  26. `residence_place` - Місце проживання
  27. `registration_place` - Місце реєстрації
  28. `driver_license_file` - Водійське посвідчення (файл)
  29. `id_certificate_file` - Посвідчення особи (файл)
  30. `foreign_passport_number` - Номер закордонного паспорта
  31. `foreign_passport_issue_date` - Дата видачі закордонного паспорта
  32. `foreign_passport_file` - Закордонний паспорт (файл)
  33. `criminal_record_file` - Довідка про несудимість (файл)
  34. `phone` - Телефон
  35. `phone_note` - Примітка до телефону
  36. `education` - Освіта
  37. `notes` - Примітка
  38. `vacation_start_date` - Дата початку відпустки (YYYY-MM-DD)
  39. `vacation_end_date` - Дата закінчення відпустки (YYYY-MM-DD)
  40. Решта полів документів та службових даних

- **fields_schema.csv** - **Мета-схема керування UI** (8 колонок, локальний файл в `.gitignore`):
  - `field_order` - Порядковий номер (1-40)
  - `field_name` - Технічна назва поля
  - `field_label` - Відображувана мітка (українською)
  - `field_type` - Тип введення: `text`, `select`, `textarea`, `number`, `email`, `tel`, `date`, `file`
  - `field_options` - Опції для select (розділені `|`), наприклад: `Працює|Звільнений|Відпустка`
  - `show_in_table` - Показувати в зведеній таблиці: `yes` / `no`
  - `field_group` - Група для картки співробітника
  - `editable_in_table` - Дозволити inline редагування: `yes` / `no`
  - **Щоб змінити UI, просто відредагуйте цей файл та перезавантажте сторінку!**
  - **Примітка:** Файл знаходиться в `.gitignore` для незалежності production від розробки. При першому встановленні створіть з `fields_schema.template.csv`

- **logs.csv** - Журнал аудиту всіх змін (9 колонок):
  - `log_id` - ID запису лога
  - `timestamp` - Час зміни (ISO 8601)
  - `action` - Тип операції: `CREATE`, `UPDATE`, `DELETE`
  - `employee_id` - ID співробітника
  - `employee_name` - ПІБ на момент зміни
  - `field_name` - Змінене поле (для UPDATE)
  - `old_value` - Старе значення
  - `new_value` - Нове значення
  - `details` - Опис зміни

- **dictionaries.csv** - (застарілий, замінений на `fields_schema.csv`):
  - Збережений для зворотної сумісності, але не використовується
  - Всі опції dropdown тепер в `fields_schema.csv` → `field_options`

## Детальний опис можливостей

### Dashboard

Dashboard — це головний екран, що показує статистику працівників та найближчі події відпусток.

**Картки статистики (Stat Cards):**
- Сітка з 4 колонок з кількістю працівників за статусом зайнятості (Всього, за статусами, Інше)
- Клік на картку розкриває inline-список ПІБ працівників з цим статусом
- Одночасно може бути розкрита лише одна картка (акордеон)
- Клік на ім'я працівника відкриває його картку

**Таймлайн відпусток:**
- Дві колонки з картками "Сьогодні" та "Найближчі 7 днів"
- Показує працівників, що йдуть у відпустку або повертаються, з бейджами дат
- Імена працівників — клікабельні посилання на їх картки

### Зведена таблиця

Зведена таблиця надає потужні можливості фільтрації та редагування:

**Взаємодія:**
- **Подвійний клік на комірці** - Редагування значення поля (для редагованих полів)
- **Подвійний клік на ID** - Відкриття картки співробітника
- **Множинні фільтри** - Вибір кількох значень одночасно для кожної колонки
- **Фільтр порожніх значень** - Чекбокс "(Порожньо)" для фільтрації рядків з порожніми значеннями
- **Скидання фільтрів** - Кнопка для скидання всіх активних фільтрів одним кліком

**Поведінка фільтрів:**
- Чекбокси для кожного унікального значення в select полях
- Опція порожнього значення з'являється першою в кожному фільтрі
- Множинний вибір працює за логікою OR
- Кількість активних фільтрів відображається в кнопці скидання

### Завантаження файлів

**Динамічні типи документів:** Всі поля з типом `file` в `fields_schema.csv` автоматично стають доступними для завантаження в розділі Документи.

**Можливості:**
- Завантаження PDF-документів для кожного співробітника
- Файли зберігаються з правильними іменами на основі типу поля (наприклад, `driver_license_file.pdf`, `id_certificate_file.pdf`)
- Кнопка "Відкрити папку" в розділі Документи відкриває папку співробітника в провіднику ОС
- Шляхи до файлів автоматично записуються у відповідні колонки `employees.csv`
- Тимчасова обробка файлів гарантує правильне іменування навіть при затримці даних форми

**Додавання нових типів документів:**
Просто додайте новий рядок в `fields_schema.csv` з `field_type=file` - зміни коду не потрібні!

### Відстеження відпусток

Автоматичне управління відпустками зі зміною статусів та сповіщеннями.

**Як це працює:**
- Встановлюєте `vacation_start_date` (дата початку) та `vacation_end_date` (дата закінчення) в картці співробітника
- При завантаженні сторінки система автоматично:
  - Змінює статус на значення "Відпустка" (друге значення з `employment_status` в `fields_schema.csv`) при настанні дати початку
  - Показує сповіщення про співробітників, які йдуть у відпустку сьогодні (✈️ синій)
  - Показує сповіщення про співробітників, які повертаються з відпустки сьогодні (🏢 зелений)
  - Очищає дати відпустки та відновлює перше значення з `employment_status` (зазвичай "Працює") після закінчення

**Динамічні статуси:**
- Статуси беруться з `fields_schema.csv` → `employment_status` → `field_options`
- При виході з відпустки: перше значення (наприклад: "Працює", "Активний")
- При відході у відпустку: значення що містить "відпустка" (наприклад: "Відпустка", "У відпустці")

**Сповіщення:**
- Модальне вікно з двома секціями: співробітники, що йдуть, та співробітники, що повертаються
- Кольорова індикація (синій для початку, зелений для повернення)
- Відображення імені, посади та дати закінчення відпустки
- З'являється автоматично при завантаженні сторінки, якщо є події сьогодні

**Ручне втручання не потрібне** - всі зміни статусів відбуваються автоматично!

### Імпорт CSV

Шаблон доступний в інтерфейсі або за шляхом `data/employees_import_sample.csv`.

**Важливо:** Файл шаблону містить UTF-8 BOM та коректне кодування для сумісності з Excel. Використовуйте його як зразок для правильного форматування.

**Правила імпорту:**
- Кодування UTF-8 з BOM, роздільник `;`, перший рядок — заголовки
- Заголовки повинні збігатися з англійськими назвами колонок з `employees.csv`
- Можна використовувати тільки частину колонок - пропущені поля будуть порожніми
- Обов'язкові поля: хоча б `last_name` або `first_name`
- `employee_id` опціональний:
  - Якщо порожній: генерується автоматично (наступний номер по порядку)
  - Якщо заповнений і вже існує: рядок пропускається
- Формат дати: `YYYY-MM-DD` (наприклад, `foreign_passport_issue_date`)
- Імпортуються всі поля співробітника: особисті дані, робота, контакти (phone, phone_note), освіта, документи тощо

### Управління довідниками

Всі значення випадаючих списків у формах керуються через `data/dictionaries.csv`:

1. Відкрийте `dictionaries.csv` в Excel
2. Додайте нові рядки з полями:
   - `dictionary_id` - наступний номер по порядку
   - `dictionary_type` - тип довідника (gender, blood_group тощо)
   - `value` - значення для збереження в базі
   - `label` - відображуваний текст
   - `order_index` - порядок сортування
3. Збережіть файл з кодуванням UTF-8 BOM
4. Оновіть сторінку в браузері

## API Endpoints

- `GET /api/employees` - Список всіх співробітників
- `GET /api/employees/:id` - Отримати дані співробітника (повертає об'єкт співробітника)
- `POST /api/employees` - Створити співробітника (приймає об'єкт співробітника)
- `PUT /api/employees/:id` - Оновити співробітника (приймає об'єкт співробітника)
- `DELETE /api/employees/:id` - Видалити співробітника
- `POST /api/employees/:id/files` - Завантажити PDF-документ
- `DELETE /api/employees/:id/files/:fieldName` - Видалити документ співробітника
- `POST /api/employees/:id/open-folder` - Відкрити папку з документами співробітника в провіднику ОС
- `POST /api/employees/import` - Масовий імпорт з CSV
- `GET /api/dictionaries` - Отримати всі довідкові дані
- `GET /api/fields-schema` - Отримати динамічну схему UI (типи полів, мітки, опції)
- `POST /api/open-data-folder` - Відкрити папку data в провіднику ОС

## Розгортання на production

### Початкове встановлення

```bash
git clone <repository-url>
cd crm_manufactur
cp data/fields_schema.template.csv data/fields_schema.csv
./run.sh
```

### Оновлення з GitHub

Оскільки `fields_schema.csv` знаходиться в `.gitignore`, ваші локальні зміни схеми не конфліктуватимуть з оновленнями:

```bash
git pull origin master
./stop.sh
./run.sh
```

**Автоматична міграція схеми:**
- При запуску сервер автоматично порівнює `fields_schema.csv` з `employees.csv`
- Якщо в схемі є нові поля - вони додаються в `employees.csv` з порожніми значеннями
- Всі існуючі дані зберігаються без змін
- Міграція відбувається прозоро при першому старті після оновлення

**Ручне оновлення схеми (опціонально):**
Якщо в новій версії додані важливі поля в `fields_schema.template.csv`:

```bash
# Подивіться зміни в template
git diff data/fields_schema.template.csv

# Вручну додайте потрібні поля у ваш локальний fields_schema.csv
nano data/fields_schema.csv
```

## Розробка

Для детальної архітектури та рекомендацій з розробки див. [CLAUDE.md](CLAUDE.md).

## Ліцензія

MIT
