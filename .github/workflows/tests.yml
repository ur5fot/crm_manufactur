name: Tests

on:
  push:
    branches: [ master, feature/* ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install root dependencies
        run: npm install

      - name: Install server dependencies
        working-directory: ./server
        run: npm install

      - name: Install client dependencies
        working-directory: ./client
        run: npm install

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Setup test data files
        run: |
          mkdir -p data files
          cp data/fields_schema.template.csv data/fields_schema.csv
          cp data/config.template.csv data/config.csv

      - name: Generate import sample from schema
        working-directory: ./server
        run: node src/sync-template.js

      - name: Run unit tests
        working-directory: ./server
        run: |
          node test/config.test.js
          node test/upload-limit.test.js
          node test/docx-generator.test.js
          node test/declension.test.js
          node test/retirement-events.test.js
          node test/utils.test.js
          node test/reprimands-store.test.js

      - name: Start backend server
        working-directory: ./server
        run: |
          nohup npm run dev > ../server.log 2>&1 &
          echo $! > ../server.pid

      - name: Start frontend server
        working-directory: ./client
        run: |
          nohup npm run dev > ../client.log 2>&1 &
          echo $! > ../client.pid

      - name: Wait for servers to be ready
        run: |
          echo "Waiting for backend..."
          backend_ready=false
          for i in {1..30}; do
            if curl -s http://localhost:3000/api/employees > /dev/null; then
              echo "Backend ready"
              backend_ready=true
              break
            fi
            echo "Attempt $i: Backend not ready yet..."
            sleep 2
          done
          if [ "$backend_ready" = false ]; then
            echo "ERROR: Backend failed to start after 30 attempts"
            cat server.log || true
            exit 1
          fi

          echo "Waiting for frontend..."
          frontend_ready=false
          for i in {1..30}; do
            if curl -s http://localhost:5173 > /dev/null; then
              echo "Frontend ready"
              frontend_ready=true
              break
            fi
            echo "Attempt $i: Frontend not ready yet..."
            sleep 2
          done
          if [ "$frontend_ready" = false ]; then
            echo "ERROR: Frontend failed to start after 30 attempts"
            cat client.log || true
            exit 1
          fi

      - name: Run API integration tests
        working-directory: ./server
        run: |
          node test/templates-api.test.js
          node test/retirement-api.test.js
          node test/search-api.test.js
          node test/photo-api.test.js
          node test/status-history.test.js
          node test/reprimands-api.test.js

      - name: Run Playwright tests
        run: npm run test:e2e

      - name: Upload test artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7

      - name: Upload server logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: server-logs
          path: |
            server.log
            client.log
          retention-days: 7

      - name: Stop servers
        if: always()
        run: |
          if [ -f server.pid ]; then kill $(cat server.pid) || true; fi
          if [ -f client.pid ]; then kill $(cat client.pid) || true; fi
