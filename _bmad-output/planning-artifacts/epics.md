---
stepsCompleted:
  - step-01-validate-prerequisites
  - step-02-design-epics
  - step-03-create-stories
  - step-04-final-validation
inputDocuments:
  - _bmad-output/planning-artifacts/prd.md
  - _bmad-output/planning-artifacts/architecture.md
  - _bmad-output/planning-artifacts/ux-design-specification.md
---

# crm_manufactur - Epic Breakdown

## Overview

This document provides the complete epic and story breakdown for crm_manufactur, decomposing the requirements from the PRD, UX Design, and Architecture requirements into implementable stories.

## Requirements Inventory

### Functional Requirements

**Dashboard та огляд (FR1-FR6):**
- FR1: Адміністратор може бачити Dashboard як домашню сторінку при відкритті системи
- FR2: Адміністратор може бачити статистику працівників по статусах (працює, у відпустці, на лікарняному, звільнений) з кількістю в кожній категорії
- FR3: Адміністратор може бачити блок "Сьогодні" зі списком працівників, які сьогодні йдуть або повертаються з відпустки
- FR4: Адміністратор може бачити блок "Цього тижня" зі списком найближчих подій відпусток
- FR5: Система автоматично оновлює дані Dashboard кожні 5 хвилин без перезавантаження сторінки
- FR6: Адміністратор може перейти з Dashboard до зведеної таблиці одним кліком

**Повідомлення та нагадування (FR7-FR9):**
- FR7: Система показує модальне вікно з подіями відпусток при завантаженні сторінки
- FR8: Модальне вікно відображає дві секції: працівники, що йдуть у відпустку сьогодні, та працівники, що повертаються сьогодні
- FR9: Адміністратор може закрити модальне вікно та продовжити роботу

**Звіти та експорт (FR10-FR13):**
- FR10: Адміністратор може згенерувати звіт "Хто у відпустці зараз" одним кліком
- FR11: Адміністратор може згенерувати звіт "Хто йде у відпустку цього місяця" одним кліком
- FR12: Адміністратор може експортувати результати поточної фільтрації зведеної таблиці у CSV файл
- FR13: Експортований CSV файл зберігає UTF-8 BOM та роздільник `;` для сумісності з Excel

**Навігація та перегляд (FR14-FR15):**
- FR14: Адміністратор може перемикатися між режимами перегляду: Dashboard, Картки, Таблиця, Логи
- FR15: Система запам'ятовує Dashboard як режим перегляду за замовчуванням при завантаженні

**Управління відпустками - існуюча функціональність (FR16-FR21, FR27):**
- FR16: Адміністратор може знайти працівника через пошук *(вже реалізовано)*
- FR17: Адміністратор може встановити дати початку та закінчення відпустки в картці працівника *(вже реалізовано)*
- FR18: Система автоматично змінює статус працівника на "Відпустка" при настанні дати початку *(вже реалізовано)*
- FR19: Система автоматично повертає статус працівника після закінчення дати відпустки *(вже реалізовано)*
- FR20: Адміністратор може редагувати дати відпустки після їх встановлення *(вже реалізовано)*
- FR21: Адміністратор може вручну змінити статус працівника, перевизначивши автоматичний *(вже реалізовано)*
- FR27: Система автоматично розраховує та відображає кількість календарних днів відпустки на основі дат початку та закінчення *(НОВА)*

**Фільтрація даних - існуюча функціональність (FR22-FR23):**
- FR22: Адміністратор може фільтрувати зведену таблицю за кількома критеріями одночасно *(вже реалізовано)*
- FR23: Адміністратор може фільтрувати за полями типу select за допомогою чекбоксів *(вже реалізовано)*

**Аудит та цілісність (FR24-FR26):**
- FR24: Система логує всі зміни даних працівників з міткою часу, полем, старим та новим значенням *(вже реалізовано)*
- FR25: Адміністратор може переглядати журнал змін *(вже реалізовано)*
- FR26: Система зберігає цілісність CSV файлів при запису (атомарний запис)

### NonFunctional Requirements

**Продуктивність (NFR1-NFR6):**
- NFR1: Dashboard завантажується менше ніж за 2 секунди при повному наборі даних (~120 працівників)
- NFR2: Пошук працівника повертає результати менше ніж за 5 секунд
- NFR3: Генерація звіту (відпустки зараз/цього місяця) завершується менше ніж за 30 секунд
- NFR4: Експорт результатів фільтрації у CSV файл завершується менше ніж за 5 секунд
- NFR5: Відповідь API на всі endpoints менше ніж за 500мс при нормальному навантаженні
- NFR6: Авто-рефреш Dashboard кожні 5 хвилин не впливає на інтерактивність інтерфейсу (фоновий запит)

**Цілісність даних (NFR7-NFR10):**
- NFR7: CSV файли записуються атомарно (запис у тимчасовий файл → перейменування) для запобігання втраті даних при збоях
- NFR8: UTF-8 BOM зберігається при кожному записі CSV файлу для сумісності з Excel
- NFR9: Роздільник `;` залишається незмінним у всіх CSV операціях (запис, експорт, імпорт)
- NFR10: 100% точність розрахунків кількості днів відпустки на основі дат початку та закінчення

### Additional Requirements

**З Architecture Document:**
- Brownfield project — розширення існуючої кодової бази Vue.js 3 + Express.js, без міграцій
- Starter template: НЕ ПОТРІБНИЙ (brownfield) — перша story починається з розширення існуючого коду
- Нові API endpoints: `GET /api/dashboard/stats`, `GET /api/dashboard/events`, `GET /api/reports/vacations`, `GET /api/export`
- Tab Bar navigation замість існуючого view switcher (3 кнопки → 4 tabs)
- Dashboard як default view (`currentView === 'dashboard'`)
- Inline expand для stat cards (список імен при кліку по картці)
- Auto-refresh з setInterval (5 хвилин), активний тільки при Dashboard view
- Vacation days calculation — client-side computed property
- CSV Export — server-side generation через csv-stringify з UTF-8 BOM + `;`
- Compute-on-read стратегія для всіх нових endpoints (без кешування)
- Окремі .vue компоненти для Dashboard дозволені якщо > 100-150 рядків або логічно ізольовані
- Всі нові стилі додаються до styles.css (без окремих CSS файлів)
- API response format: прямий масив/об'єкт без wrapper
- Backend шари: index.js (routes) → store.js (logic) → csv.js (I/O), routes НЕ читають CSV напряму

**З UX Design Specification:**
- Tab bar: 4 tabs (Dashboard, Cards, Table, Logs), height 48px, active tab з border-bottom
- Stat cards: Grid 2×2 layout, кольорове кодування статусів (green/blue/yellow/gray)
- Stat card anatomy: Number (36px bold) + Label (14px) + Color accent (4px left border)
- Inline expand: клік по stat card → список імен з'являється під карткою, transition 200ms
- Timeline sections: "Сьогодні" / "Цього тижня" з emoji та ПІБ
- Timestamp footer: "Оновлено: HH:MM" (12px, сірий)
- Empty states: позитивне повідомлення "Нічого термінового" замість порожнечі
- Кольорова палітра статусів: Працює (#4CAF50), Відпустка (#2196F3), Увага (#FF9800), Звільнений (#9E9E9E)
- Базова палітра: Фон (#FAFAFA), Картка (#FFFFFF), Бордер (#E0E0E0), Primary action (#1976D2)
- Dashboard max-width: 960px, центрування по горизонталі
- Модальне вікно нагадувань: backdrop rgba(0,0,0,0.3), max-width 500px

### FR Coverage Map

| FR | Epic | Story | Опис |
|----|------|-------|------|
| FR1 | Epic 1 | 1.2 | Dashboard як домашня сторінка |
| FR2 | Epic 1 | 1.2 | Статистика працівників по статусах |
| FR3 | Epic 2 | 2.1 | Блок "Сьогодні" |
| FR4 | Epic 2 | 2.1 | Блок "Цього тижня" |
| FR5 | Epic 1 | 1.3 | Авто-рефреш кожні 5 хвилин |
| FR6 | Epic 2 | 2.2 | Перехід Dashboard → Table |
| FR7 | Epic 2 | 2.3 | Модальне вікно при завантаженні |
| FR8 | Epic 2 | 2.3 | Дві секції в модальному вікні |
| FR9 | Epic 2 | 2.3 | Закриття модального вікна |
| FR10 | Epic 3 | 3.1 | Звіт "Хто у відпустці зараз" |
| FR11 | Epic 3 | 3.1 | Звіт "Відпустки цього місяця" |
| FR12 | Epic 3 | 3.2 | Експорт фільтрації в CSV |
| FR13 | Epic 3 | 3.2 | CSV з UTF-8 BOM + `;` |
| FR14 | Epic 1 | 1.1 | Перемикання між 4 views |
| FR15 | Epic 1 | 1.2 | Dashboard за замовчуванням |
| FR27 | Epic 3 | 3.3 | Розрахунок днів відпустки |
| FR16-FR26 | — | — | Вже реалізовано (не в scope) |

## Epic List

### Epic 1: Dashboard як домашня сторінка
Олена відкриває систему і одразу бачить повну картину стану 120 працівників — статистику по статусах, без жодного кліку.

**FRs covered:** FR1, FR2, FR5, FR14, FR15
**Stories:** 1.1, 1.2, 1.3

---

### Epic 2: Події відпусток та нагадування
Олена бачить хто сьогодні йде/повертається з відпустки, отримує автоматичні нагадування, може розкрити деталі одним кліком прямо на Dashboard.

**FRs covered:** FR3, FR4, FR6, FR7, FR8, FR9
**Stories:** 2.1, 2.2, 2.3

---

### Epic 3: Звіти та експорт даних
Олена може відповісти на запит керівництва за 2 хвилини — згенерувати звіт одним кліком та експортувати дані в Excel-сумісний CSV.

**FRs covered:** FR10, FR11, FR12, FR13, FR27
**Stories:** 3.1, 3.2, 3.3

---

## Epic 1: Dashboard як домашня сторінка

Олена відкриває систему і одразу бачить повну картину стану 120 працівників — статистику по статусах, без жодного кліку. Dashboard стає домашньою сторінкою з Tab Bar навігацією та авто-оновленням.

### Story 1.1: Tab Bar навігація

As a адміністратор,
I want перемикатися між режимами перегляду через горизонтальні вкладки (Dashboard, Картки, Таблиця, Логи),
So that я маю зручну навігацію між усіма розділами системи.

**Acceptance Criteria:**

**Given** сторінка завантажена
**When** адміністратор бачить верхню частину інтерфейсу
**Then** відображається Tab Bar з 4 вкладками: "Dashboard", "Картки", "Таблиця", "Логи"
**And** Tab Bar має висоту 48px, border-bottom 1px solid #E0E0E0

**Given** Tab Bar відображається
**When** адміністратор натискає на вкладку
**Then** активна вкладка підсвічується (#1976D2 + border-bottom 2px)
**And** вміст перемикається на відповідний view
**And** попередній view switcher (3 кнопки) замінений новим Tab Bar

**Given** адміністратор наводить курсор на неактивну вкладку
**When** hover спрацьовує
**Then** текст вкладки змінює колір на #333
**And** cursor стає pointer

**Refs:** FR14
**Technical notes:**
- Замінити існуючий view switcher на Tab Bar в App.vue
- CSS класи: `.tab-bar`, `.tab-item`, `.tab-item.active` в styles.css
- `currentView` ref розширити значенням `'dashboard'`

### Story 1.2: Dashboard API та базовий view зі Stat Cards

As a адміністратор,
I want бачити Dashboard як домашню сторінку зі статистикою працівників по статусах,
So that я одразу отримую повну картину стану персоналу при відкритті системи.

**Acceptance Criteria:**

**Given** сервер працює
**When** клієнт надсилає `GET /api/dashboard/stats`
**Then** API повертає JSON: `{ total, working, vacation, sick, fired }`
**And** числа відповідають реальній кількості працівників по `employment_status`
**And** відповідь повертається менше ніж за 500мс (NFR5)

**Given** сторінка завантажується вперше
**When** Dashboard рендериться
**Then** `currentView` за замовчуванням = `'dashboard'` (FR15)
**And** Dashboard відображається замість попереднього default view

**Given** Dashboard view активний
**When** дані завантажені
**Then** відображаються 4 Stat Cards у сітці 2×2:
- "Всього" (нейтральний колір, `#E0E0E0` left border)
- "Працює" (зелений, `#4CAF50` left border)
- "Відпустка" (синій, `#2196F3` left border)
- "Інше" (сірий, `#9E9E9E` left border — лікарняний + звільнений)
**And** кожна картка показує число (36px, bold) + label (14px)
**And** Dashboard має max-width 960px, центрований горизонтально

**Given** Dashboard view активний
**When** дані ще завантажуються
**Then** Dashboard відображається без помилок (числа 0 або loading стан)

**Given** в системі 0 працівників
**When** Dashboard рендериться
**Then** всі Stat Cards показують 0

**Refs:** FR1, FR2, FR15, NFR1, NFR5
**Technical notes:**
- Backend: `getDashboardStats()` в store.js, route в index.js
- Frontend: `api.getDashboardStats()` в api.js
- Computed properties з `employees` ref для stat cards
- CSS Grid layout: `grid-template-columns: 1fr 1fr`, gap 16px
- CSS Custom Properties для кольорів статусів

### Story 1.3: Авто-рефреш Dashboard та Timestamp Footer

As a адміністратор,
I want щоб Dashboard автоматично оновлював дані кожні 5 хвилин і показував час останнього оновлення,
So that я завжди бачу актуальну інформацію та довіряю даним на екрані.

**Acceptance Criteria:**

**Given** Dashboard view активний
**When** проходить 5 хвилин
**Then** система автоматично перезавантажує дані з сервера
**And** Stat Cards оновлюються новими числами
**And** оновлення відбувається фоново, без впливу на інтерактивність (NFR6)

**Given** Dashboard view активний
**When** дані завантажені або оновлені
**Then** внизу Dashboard відображається "Оновлено: HH:MM"
**And** текст 12px, колір #999, вирівнювання по правому краю

**Given** адміністратор перемикається з Dashboard на інший view
**When** view змінюється
**Then** авто-рефреш зупиняється (clearInterval)

**Given** адміністратор повертається на Dashboard з іншого view
**When** Dashboard стає активним
**Then** дані одразу перезавантажуються
**And** авто-рефреш запускається знову (новий setInterval)

**Refs:** FR5, NFR6
**Technical notes:**
- `setInterval` / `clearInterval` в watch на `currentView`
- Ref `lastUpdated` для timestamp footer
- Інтервал 300000мс (5 хвилин)

## Epic 2: Події відпусток та нагадування

Олена бачить хто сьогодні йде/повертається з відпустки, отримує автоматичні нагадування, може розкрити деталі одним кліком прямо на Dashboard.

### Story 2.1: Dashboard Events API та Timeline секції

As a адміністратор,
I want бачити на Dashboard блоки "Сьогодні" та "Цього тижня" зі списком подій відпусток,
So that я одразу знаю хто йде або повертається з відпустки без пошуку в таблиці.

**Acceptance Criteria:**

**Given** сервер працює
**When** клієнт надсилає `GET /api/dashboard/events`
**Then** API повертає JSON: `{ today: [...], thisWeek: [...] }`
**And** кожен елемент містить: `employee_id`, `name` (ПІБ), `type` ("vacation_start" або "vacation_end"), `date`, `end_date` (для vacation_start)
**And** відповідь повертається менше ніж за 500мс (NFR5)

**Given** Dashboard view активний і є події сьогодні
**When** дані завантажені
**Then** під Stat Cards відображається секція "Сьогодні" (заголовок 18px, font-weight 600)
**And** кожна подія показує emoji + ПІБ + опис:
- ✈️ для початку відпустки (з датою повернення)
- 🏢 для повернення з відпустки

**Given** Dashboard view активний і є події цього тижня (крім сьогодні)
**When** дані завантажені
**Then** під секцією "Сьогодні" відображається секція "Цього тижня"
**And** кожна подія показує дату + emoji + ПІБ + опис
**And** події відсортовані по даті

**Given** немає подій сьогодні
**When** Dashboard рендериться
**Then** секція "Сьогодні" показує "Нічого термінового" сірим текстом (#999)

**Given** немає подій цього тижня
**When** Dashboard рендериться
**Then** секція "Цього тижня" показує "Немає запланованих подій" сірим текстом (#999)

**Given** авто-рефреш Dashboard спрацьовує (Story 1.3)
**When** дані оновлюються
**Then** Timeline секції також оновлюються актуальними подіями

**Refs:** FR3, FR4, NFR1, NFR5
**Technical notes:**
- Backend: `getDashboardEvents()` в store.js — фільтрація по `vacation_start_date` та `vacation_end_date`
- "Цього тижня" = від завтра до кінця поточного тижня (неділя)
- Frontend: `api.getDashboardEvents()` в api.js
- CSS: `.timeline-section`, `.timeline-event` в styles.css

### Story 2.2: Inline Expand для Stat Cards

As a адміністратор,
I want натиснути на Stat Card і побачити список імен працівників з цим статусом, а також перейти до зведеної таблиці одним кліком,
So that я можу швидко дізнатись деталі та перейти до роботи з конкретними працівниками.

**Acceptance Criteria:**

**Given** Dashboard view активний
**When** адміністратор натискає на Stat Card (наприклад, "Відпустка: 15")
**Then** під карткою плавно розкривається список імен працівників з відповідним статусом
**And** анімація розкриття: transition max-height 200ms ease
**And** іконка на картці змінюється на ▲ (розкрито)

**Given** список імен розкритий для однієї картки
**When** адміністратор натискає на іншу Stat Card
**Then** попередній список згортається
**And** новий список розкривається
**And** одночасно може бути розкрита лише 1 картка

**Given** список імен розкритий
**When** адміністратор натискає на ту саму картку повторно
**Then** список згортається
**And** іконка змінюється на ▼ (згорнуто)

**Given** Dashboard view активний
**When** адміністратор натискає на Stat Card "Всього"
**Then** розкривається повний список усіх працівників

**Given** Stat Card з hover
**When** адміністратор наводить курсор на картку
**Then** cursor стає pointer
**And** картка отримує subtle shadow (box-shadow)

**Given** Dashboard view активний
**When** адміністратор натискає на вкладку "Таблиця" в Tab Bar
**Then** відкривається зведена таблиця (FR6 — перехід одним кліком)

**Refs:** FR6
**Technical notes:**
- Reactive ref `expandedCard` (null | 'total' | 'working' | 'vacation' | 'other')
- CSS: `.inline-expand`, `.stat-card.expanded` в styles.css
- `overflow: hidden; transition: max-height 200ms ease`
- Список імен: `employees.filter(e => matchStatus(e, cardType))`

### Story 2.3: Розширене модальне вікно нагадувань

As a адміністратор,
I want бачити модальне вікно з подіями відпусток при завантаженні сторінки,
So that я одразу отримую нагадування про важливі події без додаткових дій.

**Acceptance Criteria:**

**Given** сторінка завантажується і є події відпусток сьогодні
**When** дані завантажені
**Then** автоматично з'являється модальне вікно нагадувань
**And** модальне вікно має max-width 500px, центроване, backdrop rgba(0,0,0,0.3)

**Given** модальне вікно відкрите
**When** адміністратор бачить вміст
**Then** відображаються дві секції:
- ✈️ "Йдуть у відпустку сьогодні" (синій акцент) — список працівників з датою повернення
- 🏢 "Повертаються з відпустки сьогодні" (зелений акцент) — список працівників

**Given** немає працівників що йдуть у відпустку сьогодні
**When** модальне вікно відображається
**Then** секція "Йдуть у відпустку" не показується (тільки "Повертаються")

**Given** немає працівників що повертаються сьогодні
**When** модальне вікно відображається
**Then** секція "Повертаються" не показується (тільки "Йдуть у відпустку")

**Given** немає жодних подій сьогодні
**When** сторінка завантажується
**Then** модальне вікно НЕ з'являється

**Given** модальне вікно відкрите
**When** адміністратор натискає кнопку "Закрити" або Escape
**Then** модальне вікно закривається
**And** адміністратор бачить Dashboard

**Refs:** FR7, FR8, FR9
**Technical notes:**
- Розширення існуючої функції `checkVacations()` в App.vue
- Використати існуючий модальний компонент або стилі модалки
- Показувати після завантаження даних (`loadEmployees()` → `checkVacations()` → show modal)

## Epic 3: Звіти та експорт даних

Олена може відповісти на запит керівництва за 2 хвилини — згенерувати звіт одним кліком та експортувати дані в Excel-сумісний CSV.

### Story 3.1: Швидкі звіти по відпустках

As a адміністратор,
I want згенерувати звіт "Хто у відпустці зараз" або "Відпустки цього місяця" одним кліком на Dashboard,
So that я можу миттєво відповісти на запит керівництва без ручного пошуку та фільтрації.

**Acceptance Criteria:**

**Given** сервер працює
**When** клієнт надсилає `GET /api/reports/vacations?type=current`
**Then** API повертає масив працівників, які зараз у відпустці: `[{ employee_id, name, vacation_start_date, vacation_end_date, days }]`
**And** відповідь повертається менше ніж за 500мс (NFR5)

**Given** сервер працює
**When** клієнт надсилає `GET /api/reports/vacations?type=month`
**Then** API повертає масив працівників з відпустками в поточному місяці (ті хто вже у відпустці + хто йде цього місяця)
**And** генерація завершується менше ніж за 30 секунд (NFR3)

**Given** Dashboard view активний
**When** адміністратор бачить Dashboard
**Then** під Timeline секціями відображаються 2 кнопки швидких звітів:
- "Хто у відпустці зараз"
- "Відпустки цього місяця"

**Given** Dashboard view активний
**When** адміністратор натискає кнопку "Хто у відпустці зараз"
**Then** під кнопкою відображається таблиця-результат: ПІБ, дата початку, дата закінчення, кількість днів
**And** повторний клік згортає результат

**Given** Dashboard view активний
**When** адміністратор натискає кнопку "Відпустки цього місяця"
**Then** під кнопкою відображається таблиця-результат з усіма відпустками поточного місяця

**Given** немає працівників у відпустці
**When** адміністратор натискає "Хто у відпустці зараз"
**Then** відображається повідомлення "Наразі ніхто не у відпустці"

**Refs:** FR10, FR11, NFR3, NFR5
**Technical notes:**
- Backend: `getVacationReport(type)` в store.js, route в index.js
- Frontend: `api.getVacationReport(type)` в api.js
- Кнопки стилізовані як Secondary buttons
- Результат — inline таблиця під кнопками на Dashboard

### Story 3.2: Експорт фільтрації в CSV

As a адміністратор,
I want експортувати результати поточної фільтрації зведеної таблиці у CSV файл,
So that я можу передати точні дані керівництву у форматі Excel.

**Acceptance Criteria:**

**Given** сервер працює
**When** клієнт надсилає `GET /api/export` з query parameters фільтрів (status, location, department, fields)
**Then** сервер повертає CSV файл з `Content-Disposition: attachment; filename="employees_export.csv"`
**And** CSV файл має UTF-8 BOM кодування (NFR8)
**And** роздільник `;` (NFR9)
**And** експорт завершується менше ніж за 5 секунд (NFR4)

**Given** Table view активний з активними фільтрами
**When** адміністратор бачить відфільтровану таблицю
**Then** відображається кнопка "Експорт" (Primary button)

**Given** Table view з активними фільтрами
**When** адміністратор натискає кнопку "Експорт"
**Then** браузер завантажує CSV файл з даними, що відповідають поточним фільтрам
**And** файл містить лише колонки, які відображаються в таблиці (`show_in_table=yes`)

**Given** Table view без фільтрів
**When** адміністратор натискає "Експорт"
**Then** експортуються всі працівники

**Given** фільтрація повертає 0 результатів
**When** адміністратор натискає "Експорт"
**Then** завантажується CSV файл тільки з заголовками (без рядків даних)

**Given** експортований CSV файл
**When** Олена відкриває його в Excel
**Then** кирилиця відображається коректно
**And** дані розділені по колонках (роздільник `;`)

**Refs:** FR12, FR13, NFR4, NFR8, NFR9
**Technical notes:**
- Backend: `exportEmployees(filters)` в store.js, csv-stringify з BOM + `;`
- Route: `GET /api/export?status=...&location=...&department=...`
- Frontend: `api.exportCSV(currentFilters)` — ініціює download через `<a href>` або `fetch + blob`
- Кнопка розміщується поруч з існуючими елементами Table view

### Story 3.3: Розрахунок днів відпустки

As a адміністратор,
I want бачити автоматично розраховану кількість календарних днів відпустки в картці працівника,
So that я не рахую дні вручну і маю 100% точність розрахунків.

**Acceptance Criteria:**

**Given** Cards view, картка працівника відкрита
**When** заповнені обидва поля: `vacation_start_date` та `vacation_end_date`
**Then** поруч з датами відображається розрахована кількість календарних днів
**And** формат: "14 календарних днів"

**Given** картка працівника
**When** адміністратор змінює `vacation_start_date` або `vacation_end_date`
**Then** кількість днів перераховується автоматично в реальному часі
**And** 100% точність розрахунку (NFR10)

**Given** vacation_start_date = "2026-03-10", vacation_end_date = "2026-03-24"
**When** система розраховує дні
**Then** результат = 14 календарних днів

**Given** заповнена тільки одна дата (початку або закінчення)
**When** картка працівника відображається
**Then** кількість днів НЕ відображається

**Given** vacation_end_date раніше ніж vacation_start_date
**When** система розраховує дні
**Then** кількість днів НЕ відображається (некоректні дати)

**Given** обидві дати порожні
**When** картка працівника відображається
**Then** кількість днів НЕ відображається

**Refs:** FR27, NFR10
**Technical notes:**
- Client-side computed property в App.vue
- `Math.ceil((endDate - startDate) / (1000 * 60 * 60 * 24))` днів
- Відображається як текст поруч з полями дат відпустки в секції форми
