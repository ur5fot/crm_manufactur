# Исправление валидации поля birth_date

**Дата:** 2026-02-11
**Проблема:** При добавлении даты рождения (birth_date) возникала ошибка валидации для поля `insurance_file_expiry_date`
**Статус:** ✅ Исправлено и протестировано

---

## Описание проблемы

При попытке обновить поле `birth_date` для сотрудника, API возвращала ошибку:

```json
{
  "error": "Невірний формат дати для поля insurance_file_expiry_date (очікується YYYY-MM-DD)"
}
```

### Корневая причина

**Проблема 1: Валидация проверяла ВСЕ поля дат**

В endpoint `PUT /api/employees/:id` (файл `server/src/index.js`, строки 565-591) валидация проверяла **все** поля с `_date` в названии, даже если они не были изменены в текущем запросе:

```javascript
const dateFields = getEmployeeColumnsSync().filter(col =>
  col.includes('_date') || col === 'birth_date'
);

for (const dateField of dateFields) {
  const dateValue = String(next[dateField] || "").trim();
  if (dateValue) {
    // Валидация формата и корректности
  }
}
```

**Проблема 2: Невалидные legacy данные в CSV**

В файле `data/employees.csv` в некоторых полях дат были записаны имена сотрудников вместо дат:

```csv
employee_id;...;insurance_file_expiry_date
1;...;Тестовий співробітник 1
3;...;Тестовий співробітник 3
4;...;Співробітник пенсійного віку
```

Это произошло из-за смещения колонок при импорте или ручном редактировании CSV.

**Комбинация проблем:**
1. Пользователь пытается обновить `birth_date`
2. Валидация проверяет ВСЕ поля дат, включая `insurance_file_expiry_date`
3. Находит невалидное значение "Тестовий співробітник 1" в `insurance_file_expiry_date`
4. Выдает ошибку и блокирует сохранение

---

## Решение

### 1. Улучшена валидация (server/src/index.js)

**Изменение:** Валидация теперь проверяет **только измененные поля** в текущем запросе:

```javascript
// Находим какие поля были изменены в этом запросе
const changedDateFields = dateFields.filter(dateField => {
  const currentValue = String(current[dateField] || "").trim();
  const nextValue = String(next[dateField] || "").trim();
  return currentValue !== nextValue;
});

// Валидируем только измененные поля
for (const dateField of changedDateFields) {
  const dateValue = String(next[dateField] || "").trim();
  if (dateValue) {
    // Валидация формата и корректности
  }
}
```

**Преимущества:**
- ✅ Пользователь может обновить валидное поле даже если в базе есть legacy невалидные данные
- ✅ Валидация быстрее (проверяет меньше полей)
- ✅ Защита от циклических ошибок валидации

### 2. Создан скрипт очистки (server/src/clean-invalid-dates.js)

**Назначение:** Автоматическая очистка всех невалидных значений в полях дат

**Что делает скрипт:**
1. Загружает все поля с `_date` в названии из `fields_schema.csv`
2. Проходит по всем сотрудникам в `employees.csv`
3. Проверяет каждое поле даты на соответствие формату `YYYY-MM-DD`
4. Очищает (заменяет на пустую строку) невалидные значения
5. Сохраняет очищенные данные обратно в CSV

**Запуск:**
```bash
cd server
node src/clean-invalid-dates.js
```

**Результат очистки:**
```
⚠️  Строка 2, поле "insurance_file_expiry_date": "Тестовій співробітник 1" → очищено
⚠️  Строка 4, поле "insurance_file_expiry_date": "Тестовій співробітник 3" → очищено
⚠️  Строка 5, поле "insurance_file_expiry_date": "Співробітник пенсійного віку" → очищено

📊 Статистика очистки:
   Всего очищено значений: 3
   - insurance_file_expiry_date: 3 значений

✅ Данные успешно сохранены в employees.csv
```

---

## Тестирование

### Созданы автоматические тесты

**Файлы:**
- `tests/e2e/birth-date-validation.spec.js` - E2E тесты для Playwright
- `tests/test-birth-date.js` - Простые HTTP тесты (работает на Node 16+)

**Сценарии тестирования:**
1. ✅ Валидная дата рождения сохраняется успешно
2. ✅ Неправильный формат даты (DD-MM-YYYY) отклоняется
3. ✅ Невалидная календарная дата (30 февраля) отклоняется
4. ✅ Невалидная календарная дата (31 апреля) отклоняется
5. ✅ Високосный год (29 февраля 2000) принимается
6. ✅ Невисокосный год (29 февраля 2001) отклоняется
7. ✅ Очистка даты (пустая строка) работает
8. ✅ Обновление только birth_date не затрагивает другие даты

**Запуск тестов:**
```bash
# Простой тест (Node 16+)
node tests/test-birth-date.js

# E2E тест (требует Node 18+, Playwright)
npx playwright test tests/e2e/birth-date-validation.spec.js
```

**Результаты тестирования:**
```
📊 Результаты: 8 passed, 0 failed
🎉 ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО!
```

---

## Проверка вручную

```bash
# 1. Запустить серверы
./run.sh

# 2. Добавить валидную дату рождения
curl -X PUT http://localhost:3000/api/employees/1 \
  -H "Content-Type: application/json" \
  -d '{"birth_date": "1990-05-15"}'
# Ожидается: HTTP 200, birth_date сохранено

# 3. Попытаться добавить невалидную дату
curl -X PUT http://localhost:3000/api/employees/1 \
  -H "Content-Type: application/json" \
  -d '{"birth_date": "2000-02-30"}'
# Ожидается: HTTP 400, ошибка "Невірна календарна дата"
```

---

## Рекомендации

### Для пользователей

1. **Формат даты:** Всегда используйте формат `YYYY-MM-DD` (например, `1990-05-15`)
2. **Валидные даты:** Убедитесь что дата существует (нет 30 февраля, 31 апреля и т.д.)
3. **Високосный год:** 29 февраля разрешено только для високосных годов (2000, 2004, 2020, ...)

### Для разработчиков

1. **Миграция данных:** При обнаружении невалидных дат запустите скрипт очистки:
   ```bash
   cd server && node src/clean-invalid-dates.js
   ```

2. **Импорт CSV:** Убедитесь что при импорте CSV колонки правильно выровнены с заголовками

3. **Валидация на фронтенде:** Рассмотрите добавление HTML5 валидации `<input type="date">` для предотвращения ввода невалидных дат

---

## Связанные файлы

**Изменены:**
- `server/src/index.js` - улучшена валидация дат (строки 565-591)

**Созданы:**
- `server/src/clean-invalid-dates.js` - скрипт очистки невалидных дат
- `tests/test-birth-date.js` - автоматические HTTP тесты
- `tests/e2e/birth-date-validation.spec.js` - E2E тесты для Playwright
- `docs/fixes/2026-02-11-birth-date-validation-fix.md` - этот документ

**Очищены:**
- `data/employees.csv` - удалены невалидные значения из полей дат

---

## Заключение

Проблема полностью решена:
- ✅ Валидация улучшена (проверяет только измененные поля)
- ✅ Legacy данные очищены (3 невалидных значения удалены)
- ✅ Создан инструмент для очистки на будущее
- ✅ Написаны автоматические тесты (8/8 passed)
- ✅ Протестировано вручную (все сценарии работают)

Пользователи теперь могут свободно добавлять и редактировать даты рождения без ошибок валидации.
