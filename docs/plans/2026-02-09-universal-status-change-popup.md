# Universal Status Change Popup

## Overview

Замена блока "Відпустка" на универсальный попап "Зміна статусу роботи" с датами и оповещениями.

Вместо отдельного блока "Відпустка" в карточке сотрудника, `employment_status` меняется через попап с выбором статуса, датой старта (обязательно) и датой завершения (опционально). Автоматика и оповещения работают для ВСЕХ статусов с датами, а не только для отпуска. Таймлайн на дашборде показывает все события смены статусов.

## Context

- **Files involved:**
  - Modify: `client/src/App.vue` (основная логика, попап, таймлайн, убрать блок Відпустка)
  - Modify: `data/fields_schema.template.csv` (переименовать vacation_start/end -> status_start/end_date, убрать группу Відпустка)
  - Modify: `server/src/store.js` (getDashboardEvents - универсальные события статусов)
  - Modify: `server/src/index.js` (адаптация endpoint если нужно)
  - Modify: `CLAUDE.md`, `README.md`, `README.uk.md` (обновить документацию)
- **Related patterns:** Динамическая схема из fields_schema.csv, позиционная конвенция статусов employment_status, существующий паттерн vacation notifications
- **Dependencies:** Нет внешних зависимостей

## Approach

- Следуем существующей архитектуре: динамическая схема, позиционная конвенция статусов
- Переиспользуем паттерн vacation notification для универсальных уведомлений
- Данные в CSV: переименовываем `vacation_start_date`/`vacation_end_date` -> `status_start_date`/`status_end_date`
- Логика `checkVacations()` обобщается до `checkStatusChanges()` — работает для любого статуса с датами
- **Testing approach**: Regular (code first, then manual testing)
- Complete each task fully before moving to the next

## Tasks

### Task 1: Schema and Data Model Changes

**Files:**
- Modify: `data/fields_schema.template.csv`
- Modify: `server/src/schema.js` (if static references exist)

**Steps:**
- [x] В `fields_schema.template.csv`: переименовать `vacation_start_date` -> `status_start_date`, label "Дата початку статусу"
- [x] В `fields_schema.template.csv`: переименовать `vacation_end_date` -> `status_end_date`, label "Дата завершення статусу"
- [x] Убрать группу "Відпустка" — поля `status_start_date`/`status_end_date` не показываются в форме (field_group пустой или специальный скрытый)
- [x] `show_in_table=no`, `editable_in_table=no` для обоих полей дат
- [x] Проверить `server/src/schema.js` — обновить EMPLOYEE_COLUMNS если там есть статические ссылки на vacation_*
- [x] Запустить сервер, убедиться что `/api/fields-schema` возвращает обновлённые поля

### Task 2: Server-side — getDashboardEvents and Status Logic

**Files:**
- Modify: `server/src/store.js`
- Modify: `server/src/index.js`

**Steps:**
- [x] В `store.js`: `getDashboardEvents()` — искать `status_start_date`/`status_end_date` вместо `vacation_*`
- [x] Добавить поле `status_type` к событиям (значение `employment_status` сотрудника)
- [x] Адаптировать описания событий — вместо "початок відпустки" использовать значение статуса из данных
- [x] В `index.js`: проверить endpoint `/api/reports/vacations` — адаптировать или убрать если не используется в UI
- [x] Проверить что API возвращает корректные события для всех типов статусов

### Task 3: Frontend — Status Change Popup

**Files:**
- Modify: `client/src/App.vue`

**Steps:**
- [x] Убрать блок "Відпустка" из карточки сотрудника (секция с vacation_start_date, vacation_end_date)
- [x] Сделать поле `employment_status` в карточке readonly (не редактируемое напрямую)
- [x] Добавить кнопку "Змінити статус" рядом с `employment_status` в карточке
- [x] Создать попап "Зміна статусу роботи":
  - Select со статусами (все `employment_status` options кроме `options[0]` — "Працює" не выбирается, это нормальное состояние)
  - Дата старта (обязательно, input type=date)
  - Дата завершення (опционально, input type=date)
  - Кнопки "Застосувати" и "Скасувати"
- [x] При применении: обновить `employment_status`, `status_start_date`, `status_end_date` через PUT API
- [x] Добавить кнопку "Скинути статус" — возвращает `options[0]`, очищает даты
- [x] Стилизация попапа в том же стиле что существующие модалки (notification modal)

### Task 4: Frontend — Universal checkStatusChanges()

**Files:**
- Modify: `client/src/App.vue`

**Steps:**
- [x] Переименовать `checkVacations()` -> `checkStatusChanges()`
- [x] Логика: если `status_start_date` <= сегодня и `status_end_date` пуста или >= сегодня — убедиться что статус установлен
- [x] Логика: если `status_end_date` < сегодня — вернуть `options[0]`, очистить `status_start_date` и `status_end_date`
- [x] Обновить все ссылки на vacation_start_date/vacation_end_date -> status_start_date/status_end_date
- [x] Обновить computed свойства: `workingStatus`, `vacationStatus` -> обобщить (vacationStatus может остаться как options[2] для эмодзи)

### Task 5: Frontend — Universal Notifications

**Files:**
- Modify: `client/src/App.vue`

**Steps:**
- [x] Обновить notification modal — заголовок "Сповіщення про зміну статусів"
- [x] Секция "Сьогодні змінюють статус:" — список сотрудников с новым статусом и датой завершения
- [x] Секция "Сьогодні повертаються:" — список тех кто возвращается к options[0]
- [x] Эмодзи по позиции статуса: options[2] (отпуск) — ✈️, options[3] (лікарняний) — 🏥, остальные — ℹ️
- [x] Стилизация секций — цвета по типу статуса (как сейчас синий для отпуска, зелёный для возвращения)

### Task 6: Dashboard Timeline — Universal Events

**Files:**
- Modify: `client/src/App.vue`

**Steps:**
- [x] Таймлайн показывает ВСЕ события смены статусов с датами (не только отпуск)
- [x] Описания адаптированы: показывать значение статуса из данных ("Відпустка", "Лікарняний" и т.д.)
- [x] Эмодзи по типу статуса (по позиции в options)
- [x] Проверить что "Сьогодні" и "Найближчі 7 днів" работают корректно с новыми данными

### Task 7: Documentation

**Files:**
- Modify: `CLAUDE.md`
- Modify: `README.md`
- Modify: `README.uk.md`

**Steps:**
- [x] Обновить CLAUDE.md: секции про vacation tracking -> status change system, vacation_start/end_date -> status_start/end_date
- [x] Обновить CLAUDE.md: описание попапа, новая логика checkStatusChanges
- [x] Обновить README.md: описание функционала смены статусов
- [x] Обновить README.uk.md: то же самое на украинском

## Verification

- [x] Ручной тест: открыть карточку сотрудника, нажать "Змінити статус", выбрать отпуск с датами, применить — статус и даты обновляются
- [x] Ручной тест: установить status_end_date = вчера, перезагрузить — статус автоматически сбрасывается на options[0]
- [x] Ручной тест: установить status_start_date = сегодня — появляется уведомление
- [x] Ручной тест: дашборд таймлайн показывает события для разных статусов (отпуск, лікарняний)
- [x] Ручной тест: кнопка "Скинути статус" возвращает options[0] и очищает даты
- [x] Ручной тест: блок "Відпустка" больше не отображается в карточке
- [x] Запустить приложение через `./run.sh` — всё работает без ошибок

## Post-completion

- [ ] Обновить README.md если user-facing changes
- [ ] Обновить CLAUDE.md если internal patterns changed
- [ ] Переместить этот план в `docs/plans/completed/`
