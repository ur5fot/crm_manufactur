# Показувати кількість доган і відзнак на кнопці

## Overview
Відображати кількість записів (доган/відзнак) прямо на кнопці "📋 Догани та відзнаки" у картці співробітника — щоб одразу видно, чи є записи без відкриття попапу. Наприклад: `📋 Догани та відзнаки (3)`.

## Context (from discovery)
- Кнопка знаходиться: `client/src/views/EmployeeCardsView.vue` (~рядок 657)
- Composable: `client/src/composables/useReprimands.js`
- `reprimands` (ref-масив) вже є в composable і вже імпортується у вью
- Проблема: наразі `reprimands` завантажується тільки при кліку (відкритті попапу)
- `loadReprimands` НЕ екпортується з composable (тільки `openReprimandsPopup`)
- `selectedId` є у `useEmployeeForm.js`, і view його вже має
- При виборі нового співробітника викликається `selectEmployee(id)` у вью

## Development Approach
- **Testing approach**: Regular (спочатку код, потім тести)
- Мінімальні зміни: тільки composable + шаблон

## Implementation Steps

### Task 1: Експортувати `loadReprimands` і завантажувати при виборі співробітника

- [x] у `client/src/composables/useReprimands.js` додати `loadReprimands` в `return` об'єкт
- [x] у `client/src/views/EmployeeCardsView.vue` деструктурувати `loadReprimands` з `useReprimands()`
- [x] додати `watch(selectedId, ...)` (або `watch(() => selectedId.value, ...)`) — при зміні id викликати `loadReprimands(newId)`, при `null/''` очищати `reprimands.value = []`
- [x] у шаблоні кнопки (рядок ~663) додати лічильник: `{{ reprimands.length > 0 ? ` (${reprimands.length})` : '' }}`
- [x] перевірити вручну: відкрити картку співробітника — на кнопці видно кількість; відкрити попап — кількість не змінилась; закрити попап — кількість оновилась після CRUD

### Task 2: Verify acceptance criteria
- [ ] кнопка показує `(N)` якщо є записи, не показує нічого якщо 0
- [ ] перехід між співробітниками оновлює лічильник
- [ ] після додавання/видалення запису в попапі — лічильник оновлюється
- [ ] `isNew` (новий співробітник) — лічильника немає (кнопка схована через `v-if="!isNew"`)

## Technical Details
- `watch(selectedId, async (newId) => { if (newId) await loadReprimands(newId); else reprimands.value = []; }, { immediate: true })`
- Або простіше — викликати `loadReprimands` всередині вже наявного watch на `route.params.id` у вью

## Post-Completion
- Ручне тестування в браузері після `./run.sh`
